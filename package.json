{
  "name": "marble-site",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-changelog-data.js",
    "dev": "node server.js",
    "update:changelog": "node scripts/generate-changelog-data.js"
  }
}
